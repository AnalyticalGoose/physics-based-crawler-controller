# physics-based-crawler-controller

A minimalist grid-based dungeon crawler movement controller using physics. Collision is controlled via raycast and static bodies however it will support non-static collision too. The controller itself is barebones and does not include queueing for example.
